[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mac-tm-doctor"
version = "0.1.0"
description = "A tiny macOS helper that diagnoses Time Machine backup blockers on attached volumes."
readme = "README.md"
license = "MIT"
requires-python = ">=3.8"
authors = [{name = "zhuhroscar-tech", email = "noreply@example.com"}]
dependencies = []
classifiers = [
    "Environment :: MacOS X",
    "Intended Audience :: End Users/Desktop",
    "Programming Language :: Python :: 3",
    "Operating System :: MacOS :: MacOS X",
]

[project.urls]
Homepage = "https://github.com/zhuhroscar-tech/mac-tm-doctor"

[project.scripts]
mac-tm-doctor = "mac_tm_doctor.cli:main"

[project.optional-dependencies]
test = ["pytest"]

[tool.setuptools]
package-dir = {"" = "src"}
